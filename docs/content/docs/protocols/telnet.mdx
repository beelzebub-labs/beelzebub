---
title: TELNET Deception
description: Emulate terminal-based devices and legacy systems with authentication and command responses.
---

Use `serverName` and `passwordRegex` for the login flow, then define ordered command handlers. TELNET also supports LLM-powered sessions and the common timeout, plugin, tracing, and safety controls.
