<div class="beyond-runtime not-prose">
  <p class="beyond-runtime__intro">
    <strong>The open-source runtime remains the foundation.</strong> You can deploy,
    extend, and operate it independently. Beelzebub Platform is the managed path
    for teams that need to coordinate deception across environments and connect
    runtime evidence to a broader security workflow.
  </p>

  <a
    href="https://beelzebub.ai/products/beelzebub-cloud/"
    target="_blank"
    rel="noreferrer"
  >
    Explore Beelzebub Platform
  </a>
</div>

<style>
  .beyond-runtime {
    margin-block: 1rem 2rem;
    border: 1px solid color-mix(in srgb, var(--color-fd-primary) 32%, var(--color-fd-border));
    border-radius: 0.9rem;
    background:
      linear-gradient(135deg, color-mix(in srgb, var(--color-fd-primary) 9%, transparent), transparent 58%),
      var(--color-fd-card);
    padding: clamp(1.1rem, 3vw, 1.5rem);
  }

  .beyond-runtime__intro {
    margin: 0 0 1.5rem;
    max-width: 76ch;
    color: var(--color-fd-muted-foreground);
    line-height: 1.65;
  }

  .beyond-runtime__intro strong {
    color: var(--color-fd-foreground);
  }

  .beyond-runtime a {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    border-radius: 0.5rem;
    background: var(--color-fd-primary);
    color: var(--color-fd-primary-foreground);
    padding: 0.5rem 0.75rem;
    font-size: 0.9rem;
    font-weight: 650;
    text-decoration: none;
  }

  .beyond-runtime a:hover {
    filter: brightness(1.08);
  }
</style>
